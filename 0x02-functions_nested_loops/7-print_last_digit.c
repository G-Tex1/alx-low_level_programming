#include "main.h"

/**
 * print_last_digit - prints the last digit of a number
 * Return: Returns the number
 * @int: Contains value
 */

int print_last_digit(int)
{
	int n;

	if (n >= 0)
	{
		result = n % 10;
		return (result);
	}
	else
	{
		result = n % 10;
		return (result);
	}
}

#include "main.h"
/**
 * add - adds two integers
 * @a: integer a stores the argument
 * @b: integer b stores the second argument
 * Return: Returns the result
 */

int add(int a, int b)
{
	int t;

	t = a + b;
	return (t);
}

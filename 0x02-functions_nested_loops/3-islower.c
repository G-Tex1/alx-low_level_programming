#include "main.h"
/**
 * _islower - c is lower
 * Return: Returns 1 if c is lower. Else it returns 0
 * 'c' : numeric value of lowercase alphabets
 */
int _islower(void)
{
	int c;

	if (c >= 97)
	{
		return (1);
	}

	else
	{
		return (0);
	}
}

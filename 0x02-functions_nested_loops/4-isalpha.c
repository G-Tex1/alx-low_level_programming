#include "main.h"
/**
 * _isalpha - prints lower and upper case alphabets
 * @c: Takes in the character
 * Return: Returns 1 or 0
 */

int _isalpha(int c)
{
	if (c >= 65)
	{
		return (1);
	}


	else
	{
		return (0);
	}
}

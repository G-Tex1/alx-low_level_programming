#include "main.h"
/**
 * main - check code and  Prints lower case letters from a to z
 * Return: Always return 0
 */

int main(void)
{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc++)
		_putchar(lc);

	_putchar('\n');
	return (0);
}

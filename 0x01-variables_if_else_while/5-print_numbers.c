#include <stdio.h>
/**
 * main - prints digits of base 10
 * Return: returns an int
 */

int main(void)
{
	int i = 0;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (i);
}
